services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - ./logrotate.d/app:/etc/logrotate.d/app
    working_dir: /usr/src/app
    tty: true
    environment:
      ENV: local
    command: sh -c "go mod tidy && go run cmd/goat/main.go"